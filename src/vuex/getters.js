/**
 * Created by rainyiwo on 2017/8/8.
 */

const getters = {
    ownPower(state){
        // state.base.isAdmin != 1 || state.base.isAdmin != 2 || state.butler.isGold != 2 || state.base.treasure_edit != 2 || state.butler.view != 1
        //  isAdmin  管理权限--本人--1
        // isAdmin  管理权限 所有--2
        //  isGold  上上策---2
        // treasure_edit  张军金银编辑权限
        // view  浏览权限--1
    }
}

export default getters
